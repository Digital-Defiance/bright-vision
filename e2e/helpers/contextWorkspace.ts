/** @deprecated Import from `./fixtureWorkspaces` — kept for existing LLM specs. */
export {
  CONTEXT_LLM_E2E_WORKSPACE,
  E2E_CONTEXT_MAGIC,
  E2E_CONTEXT_WIDGET_REL,
  ensureContextLlmE2eWorkspace,
} from './fixtureWorkspaces'
