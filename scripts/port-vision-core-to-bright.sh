#!/usr/bin/env bash
# Retired — Vision HTTP lives in bright_vision_core/ at the BrightVision repo root.
echo "port-vision-core-to-bright.sh is retired. Use scripts/compare-cores.py and docs/CORE_FILE_MERGE.md." >&2
exit 1
